namespace fastLPI.tools.decompiler
{
    public enum OutputType
    {
        Dir,
        Zip
    }

    public enum LogLevel
    {
        ALL,
        TRACE,
        DEBUG,
        INFO,
        WARN,
        ERROR,
        OFF
    }

    public enum DumpLevel
    {
        ALWAYS,
        DEBUG,
        RELEASE,
        NEVER
    }
}
